Java Heap Space is used throughout the application, but Stack is only used for the method — or methods — currently running.
The Heap Space contains all objects are created.
Also Stack contains any reference to those objects.
Objects stored in the Heap can be accessed through the application.